import SignupModal from '@/app/(beforeLogin)/_component/SignupModal';

export default function MakeAccount() {
    return (
        <div>
            가로채기
            <SignupModal />
        </div>
    );
}
