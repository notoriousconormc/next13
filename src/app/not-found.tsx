import Link from 'next/link';
import { NextPage } from 'next';

const NotFound: NextPage = () => {
    return (
        <div>
            <div>Not Page</div>
        </div>
    );
};

export default NotFound;
